<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>

    <!-- CSS Section-->
    <link rel="stylesheet" href="/bootstrap/dist/css/bootstrap.min.css" media="print" onload="this.media='all'">
    <link rel="stylesheet" href="/@fortawesome/fontawesome-free/css/all.min.css" media="print" onload="this.media='all'">
    <link rel="stylesheet" href="/stylesheets/style.css" media="print" onload="this.media='all'">
  </head>
  <body>
    <!-- Main Navigation -->
    <nav class="navbar fixed-top navbar-expand-xl navbar-dark" style="background-color: brown;"id="header">
      <a class="navbar-brand" href="/"><i class="fas fa-poll"></i> Survey City</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
        <% if (title && title.includes("Home")) { %>
          <li class="nav-item active">
        <% } else { %>
          <li class="nav-item">
        <% } %>
          <a class="nav-link" href="/"><i class="fas fa-lg fa-laptop-house"></i> Home</a>
        </li>

        <% if (title && title.includes("Survey ")) { %>
          <li class="nav-item active">
        <% } else  { %>
          <li class="nav-item">
        <% } %>
          <a class="nav-link" href="/survey"><i class="fas fa-user-circle"></i> Surveys</a>
        </li>

        <!--
        <% if (title && title.includes("FAQ")) { %>
          <li class="nav-item active">
        <% } else  { %>
          <li class="nav-item">
        <% } %>
          <a class="nav-link" href=""><i class="fas fa-question-circle"></i> FAQs</a>
        </li>
        -->
      </div>
    </nav>
    <br>
    <!-- Main navigation ends here-->

    <!-- jumbotron-->
    <main class="container">
